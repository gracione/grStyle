import styled from "styled-components";

export const Container = styled.div`
    select, input {
        width: 100%;
        height: 7vh !important;
        font-size: 3vh;
        padding-left: 10px;
        color: black; /* Corrected the spelling from 'brack' to 'black' */
        border: none;
        border-radius: 5px;
		background: rgba(100,100,100, 0.3);
    }
`;
